export enum GamePhase {
    GETTING_READY,
    PRE_SELECTION,
    SELECTION,
    POST_SELECTION,
}

export enum SpeechBubbleLocation {
    SIDEBAR = "sidebar",
    QUIZ = "quiz",
}

export const TIP = "tip";
